<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <div class="container-fluid">
    <%= link_to'ABus',root_path,class: "navbar-brand"%>
    <!-- <a class="navbar-brand" href="#">HR-Mitra</a> -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="#">Home</a>
        </li>

        <li class="nav-item">
          <a class="nav-link" href="#">Link</a>
        </li>
        <% if user_signed_in? %>
        <% if current_user && current_user.tickets.present? %>
           <%= link_to "view Bookings" , tickets_show_path ,class:"btn btn-primary"%>
           <%end%>
        <li class="nav-item dropdown">
          <a class="nav-link active dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Setting
          </a>
          <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="javascript:void(0)">Signed in as <%= current_user.email %></a></li>
            <li><%= button_to 'sign out',destroy_user_session_path,class:'dropdown-item',method: :delete,data:{confirm: 'If you logout,your current session will be destroyed.are you sure you want to logout?'} %></li>
          </ul>
        </li>
        <% else %>
             <li class="nav-item">
              <%= link_to "Sign In",new_user_session_path,class:'nav-link active'%></li>
        <% end %>
       
      </ul>
      <form class="d-flex" role="search">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-success" type="submit">'Google Search</button>
        
      </form>
    </div>
  </div>
</nav>
